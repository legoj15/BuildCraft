package buildcraft.silicon.client;

// TODO: Implement laser beam BlockEntityRenderer using NeoForge 1.21.11's
// new render pipeline (BlockEntityRenderer<T, S> with createRenderState(),
// extractRenderState(), and submit() methods).
// The previous render() method no longer exists — the new API uses a
// separate render state object pattern with SubmitNodeCollector.
public class RenderLaser {
    // Placeholder — see TileLaser.laserPos and TileLaser.targetPos for
    // the beam endpoints that need to be rendered.
}
