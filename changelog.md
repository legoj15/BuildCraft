###### Changes since 26.1 Beta release 5:
- Fix: BC oil/fuel fluids no longer push the player upward unconditionally — entering a pool now sinks/swims like vanilla water, matching 1.12.2 behaviour. The `BCBuoyantLiquidBlock` subclass added a few commits up was forcing every `LivingEntity` inside to `dy = Math.max(delta.y * 0.8 + 0.04, 0.04)` on every tick, which (a) clamped any downward motion to +0.04 (player physically cannot fall into an oil column), and (b) added a constant +0.04 floor so even a still entity drifted upward. The override was added on the assumption that `FluidType.move()` being a dead override point left no path for vanilla buoyancy to apply, but the BC oils are aliased into `FluidTags.WATER` (via `data/minecraft/tags/fluid/water.json`) and the `FluidType.Properties` already declare `isWaterLike(true)`, `canSwim(true)`, `canPushEntity(true)`, plus per-variant `density`/`viscosity` — so vanilla `LivingEntity.travel()` already drives the right water-like sink/swim/jump behaviour without help from the block. Switched the registered block back to plain `LiquidBlock` and removed `BCBuoyantLiquidBlock`. Friction tuning still lives on `BCCustomFluidType.motionScale()` (respects the existing `oilIsSticky` config); fall-distance reset on entry now comes for free from the water-tag membership.
- Chute polish (follow-up to the connection/placement fixes one entry below): shift-clicking items into the chute's GUI now works, the chute renders connection stubs toward neighbouring inventories and item pipes, and external blocks can no longer extract from the chute (preserving the MJ-gated push behaviour from 1.12.2). (1) `ContainerChute.quickMoveStack` was a `// TODO` stub returning `ItemStack.EMPTY` — that overrode the working generic implementation in `ContainerBC_Neptune.quickMoveStack` (which routes container→player and player→container with phantom/output-slot filtering); deleting the override restores normal shift-click. (2) `TileChute.inv` switched from `EnumAccess.BOTH` to `EnumAccess.INSERT` — 1.12.2's chute exposed insertion only, so a hopper below the chute pulls nothing and the chute remains the sole item-mover (whose pushes are gated by `MjBattery.extractPower`). With BOTH, an unpowered chute could be drained by a vanilla hopper and the MJ requirement became cosmetic. The `getItemHandler(side)` capability still returns non-null under INSERT (as a `WrappedItemHandlerInsert` from `ItemHandlerManager`), so pipes still pass `canConnect` and can push items in. (3) Visual connection stubs (1.12.2's `CONNECTED_MAP` system) now ported. `BlockChute` adds 6 `connected_<side>` `BooleanProperty` (sourced from `BuildCraftProperties.CONNECTED_MAP`, the same map `BlockFloodGate` uses), recomputes the relevant face in `updateShape` whenever a neighbour changes, and seeds all 6 in `getStateForPlacement` via `computeAllConnections`. The connection check (`shouldConnect`) returns false for the intake side (FACING) — matching 1.12.2's `getActualState` exclusion — and otherwise returns true if the neighbour either exposes `Capabilities.Item.BLOCK` (vanilla inventories, registered chute-likes) or is an `IPipeHolder` whose pipe flow exposes `PipeApi.CAP_INJECTABLE` (BC item pipes). The fluid/power pipe variants do not expose `CAP_INJECTABLE`, so the chute correctly draws no stub toward them. The `chute.json` blockstate switched from a 6-variant FACING map to a 12-clause `multipart` (6 FACING rotations applied independently of 6 connection-stub rotations), exactly mirroring the 1.12.2 Forge submodel composition; new `block/chute_connected.json` model is the 6×3×6 stub at `[5,0,5]→[11,3,11]` from 1.12.2 (textured with the existing `chute/bottom` PNG already shipping in the namespace), rotated per side via the multipart's `x`/`y` apply transforms. The intake-side gate keeps the visual unchanged on the wide-opening face — only the 5 output sides ever protrude.
- Fix: Chute now connects to transport pipes, places facing the clicked side (matching 1.12.2 and other BC machines), and pushes items into both pipes and inventories. Three regressions in the 26.1 port: (1) `BCFactory.registerCapabilities` only registered the chute's MJ caps, never `Capabilities.Item.BLOCK` — pipes' `PipeFlowItems.canConnect` queries `CapUtil.CAP_ITEMS` (= `Capabilities.Item.BLOCK`) on neighbours and returned null, so no pipe ever rendered a connection toward a chute; (2) `BlockChute.getStateForPlacement` used `context.getNearestLookingDirection()` (the player's view direction), giving FACING the opposite of every other BC 6-way-facing block — `BlockEngineBase_BC8` and `BlockDynamoMJ` use `context.getClickedFace()`, and the chute's own model+blockstate (default funnel narrows down with no rotation = `facing=up`) expects FACING to be the wide-intake side, so a player looking down to place on a chest got FACING=DOWN instead of UP; (3) `TileChute.putInNearInventories` queried `Capabilities.Item.BLOCK` on every neighbour to push items, but pipes only expose `PipeApi.CAP_INJECTABLE` (registered via `PipeFlow.getCapability`, never as a vanilla block-cap) — so even after a pipe visually connected, the chute couldn't deliver. Fixes: registered `Capabilities.Item.BLOCK` for the chute BE wired through `tile.getItemHandler(direction)`, refactored the inventory to live on `itemManager.addInvHandler("inv", 4, EnumAccess.BOTH, EnumPipePart.VALUES)` so `getItemHandler` returns it (matches the `TileFilteredBuffer` pattern); switched placement to `context.getClickedFace()`; and rewrote `putInNearInventories` to call `InventoryUtil.addToRandomInjectable(level, pos, currentSide, oneItem)` first (handles BC pipes via `IInjectable` with intake-side exclusion built in), then fall back to a manual loop over the 5 non-intake sides querying `Capabilities.Item.BLOCK` for vanilla inventories. Preserves 1.12.2's 1-item-per-tick semantic by capping the test stack to 1 before each call; preserves 1.12.2's "items stay in `inv` if no acceptor" behaviour (no drop fallback). NBT key changed from `"inv"` to `"items"` to match the new `itemManager` serialization path — existing chutes lose slot contents on world load, acceptable since the chute was non-functional before this commit. Out of scope: the 1.12.2 `CONNECTED_MAP` block-state for visual connection stubs on the chute itself (cosmetic; pipes render their own connections after the capability fix).
- Fix: `FluidUtilBC.areFluidsEqual(Fluid, Fluid)` now treats source and flowing variants of the same fluid as equal, so the Flood Gate actually floods (and the Pump's infinite-water detection / world-vs-tank fluid checks actually work). Mojang split source/flowing into separate `Fluid` instances starting in 1.13 — `Fluids.WATER` and `Fluids.FLOWING_WATER` are now different objects — but the 26.1 fluid-API migration left `areFluidsEqual` as a stub that returned `a == b`, so any caller comparing a world fluid against a tank's source fluid silently returned false the moment the world cell turned flowing. The user-visible flood-gate symptom: gate places source water at its 3 immediate-neighbour air cells (delay = 16 each cycle), water flows out, the layer-2 BFS targets read as `Fluids.FLOWING_WATER`, `canFill`/`canSearch` return false against the `Fluids.WATER` tank, the BFS rebuild finds nothing it can traverse (flowing water blocks the only paths to further-out air), `delayIndex` walks 0→1→2→3→4 over ~12 s and parks at delay = 256 with the source tank still full. (1.12.2 wasn't affected because Forge 1.12.2 registered water as a single `Fluid` covering both states, and the 1.12.2 implementation compared by `getName()` either way.) Fix: compare by `Fluid.getFluidType()` — `Fluids.WATER.getFluidType()` and `Fluids.FLOWING_WATER.getFluidType()` are both `NeoForgeMod.WATER_TYPE`, and unrelated fluids have unrelated types, so identity comparison on the type cleanly groups source + flowing variants without false-positive matches across different fluids. Affects every caller of `areFluidsEqual`: `TileFloodGate.canFill`/`canSearch`/`canFillThrough`, `TilePump.canDrainBlock`/`isInfiniteWaterSource`/`canDrainThrough`. Empty-fluid handling unchanged (preserved the null-fast-path).
- Flood Gate: completed the port. Two end-user-facing pieces from the 1.12.2 version were missing in the 26.1 port — the F3 debug overlay showed nothing for the block, and wrenching a side to close/open it had no visual effect (the `BlockState` `connected_<side>` properties were toggled correctly, but `assets/buildcraftunofficial/blockstates/flood_gate.json` was a single static variant, so the texture never changed). `TileFloodGate` now implements `IDebuggable` (matching `TilePump`/`TileTank`) and exposes the same lines the 1.12.2 version did — `fluid` (via the existing `FluidUtilBC.getDebugString(FluidStacksResourceHandler)` helper), `openSides`, `delay`, `tick`, `queue size`, `paths size` — plus a client-truth `openSides (state)` line read from the synced `BlockState` properties via `BlockFloodGate.CONNECTED_MAP`. To make the F3 fluid line actually populate on the client, added `getUpdateTag`/`getUpdatePacket` overrides routing through `saveCustomOnly` (mirroring `TileTank.java:114-122`) plus a `lastSyncedAmount` guard in `serverTick` that calls `level.sendBlockUpdated` whenever the tank amount changes (covers both pipe-driven inserts and the gate's own 1000 mB extracts). The blockstate JSON is now a multipart with 11 `apply` clauses — one unconditional base for `block/flood_gate/frame` (a single element with only the `up` face textured `flood_gate/top`) plus 10 conditionals (5 directions × open/closed) each pointing at a single-face overlay model under `block/flood_gate/face_<dir>_<state>.json` that draws one quad with the appropriate `flood_gate/open` or `flood_gate/closed` texture and a matching `cullface`. Same single-face-overlay pattern vanilla redstone-wire and chiseled-bookshelf use; collision shape comes from `BlockFloodGate`'s default full-cube voxel shape (no `noOcclusion()` set), so the model having only one drawn face per direction is collision/opacity safe. The existing `models/block/flood_gate.json` is left alone — it's the parent of `models/item/flood_gate.json` and the all-sides-open render is the right inventory look. Bonus correctness fix in the wrench path: the toggle was clearing the BFS queue but leaving `delayIndex`/`tick` at their stale values, so after a side flip the gate could sit idle for up to `REBUILD_DELAYS[4]` = 256 ticks before re-planning. New `TileFloodGate.onSidesToggled()` resets all three (`queue.clear()`, `delayIndex = 0`, `tick = 0`) and is called from `BlockFloodGate.useWithoutItem` in place of the inline `queue.clear()`. The wrench's `setBlock` flag is also `Block.UPDATE_CLIENTS` (not `Block.UPDATE_ALL`) — `UPDATE_ALL`'s `UPDATE_NEIGHBORS` bit fires `BlockPipeHolder.neighborChanged` on the attached pipe (`BlockPipeHolder.java:562-570`), which calls `tile.getPipe().markForUpdate()` → `requestModelDataUpdate()` → `PipeBlockStateModel` rebake → dynamic plug renderers (e.g. `PlugPulsarRenderer`) briefly drop their overlay for one tick. The flood gate's open/closed state is purely cosmetic (pipes connect via fluid capability, which is registered on all sides unconditionally in `BCFactory.java:75-76`), so no neighbour needs to react and `UPDATE_CLIENTS` is sufficient — same visual update on the gate, no spurious pipe rebake. (The 1.12.2 version sidestepped this by using a custom `NET_RENDER_DATA` packet that didn't go through `setBlock` at all.) No new textures; reuses the three already on disk under `textures/block/flood_gate/{open,closed,top}.png`.
- Filler Planner: completed the port. Right-clicking a Volume Box corner with the Filler Planner item now attaches an `AddonFillerPlanner` addon to that slot (`ItemAddon.use` was a stub that returned PASS unconditionally — TODO comment claimed `EnumAddonSlot` and `WorldSavedDataVolumeBoxes` weren't ported yet, but they actually were; the stub was just stale). `AddonFillerPlanner` is now registered in `AddonsRegistry` (the registration call lived in `BCBuildersRegistries.java.disabled` and was never run; inlined into `BCBuilders.init`) so the addon now round-trips through NBT save/load instead of silently disappearing on chunk reload. `MenuType<ContainerFillerPlanner>` is registered in `BCBuildersMenuTypes`, screen factory is registered in `BCBuildersClient.registerMenuScreens`, and `BCBuildersGuis.openFillerPlannerGUI(player, addon)` opens the menu via `ServerPlayer.openMenu(SimpleMenuProvider, writeClientData)` carrying the volume-box UUID and `EnumAddonSlot` ordinal — the client-side `ContainerFillerPlanner` constructor reads these from the buf and looks the addon up in `ClientVolumeBoxes`. Container has full statement-pattern messaging (`NET_STATEMENT` and `NET_INVERT`) mirroring `ContainerFiller`'s scheme: client clicks pattern picker → `onStatementChange` sends `NET_STATEMENT` → server reads, applies to `addon.patternStatement`, calls `valuesChanged` (which calls `addon.updateBuildingInfo` and broadcasts the volume-box state); server `broadcastChanges` sends authoritative pattern back to client. `GuiFillerPlanner` mirrors `GuiFiller`'s pattern picker (32×32 pattern slot at (12,32), four 18×18 statement-param slots, invert toggle button at (152,40)) but drops the resource grid since the planner only configures the pattern. Item model swapped from flat `minecraft:item/generated` placeholder to `block/cube_all` referencing the existing `buildcraftunofficial:addons/filler_planner` 16×16 texture (verified byte-identical to the 1.12.2 source). `ContainerFillerPlanner.valuesChanged` and `ContainerFiller`/`TileFiller` now sync via `markDirtyAndBroadcast` so the volume-box wireframe correctly reflects the locked state on the client.
- Volume Box: ported the placeable Volume Box from 1.12.2. Right-clicking the volume_box item on a block face now spawns a wireframe box at the targeted position, rendered as a colored laser outline (`marker_volume_connected`) and persisted in `LevelSavedDataVolumeBoxes` across saves. New `MessageVolumeBoxes` `CustomPacketPayload` carries the full per-dimension box list to clients on player login, on dimension change, and whenever the server-side state changes (box added, lock applied/removed, edit-state tick). Three `NeoForge.EVENT_BUS` listeners in `BCCore` drive the sync: `LevelTickEvent.Post` calls `tick()` for editing/lock-expiry, `PlayerLoggedInEvent` sends the initial state and resumes paused edits, `PlayerChangedDimensionEvent` re-sends on dimension swap. The item model is now the 1.12.2 wireframe-cube (12 thin elements forming a hollow box) referencing `buildcraftunofficial:lasers/marker_volume_connected`, replacing the placeholder PNG. Filler/Architect lock integration that already existed continues to work — locks now propagate to the client via the same packet. Drag-edit gameplay, addon item interactions, and a volume box GUI remain unported.
- Fix: Snow / carpet / similar fragile placements where the schematic itself has air at the support position (the architect captured a vanilla-generation glitch — snow over air, snow on partial snow_layer with non-full top, etc.) now defer at queue time instead of looping through throw → undo → refund → re-queue. The `requiredBlockOffsets` dependency check is vacuously CORRECT when the schematic at the support position is air (schematic-air + world-air → CORRECT), so the position queues even though no real support exists. Added a `canSurvive` pre-check in `BlueprintBuilder.isReadyToPlace` that defers placements which would fail the post-setBlock self-destruct check anyway — this stops the wasted animation cycles. Trade-off: torch on top of a cobblestone wall whose `up=false` (a rare configuration where vanilla's `shouldRaisePost` returns false because the wall has TALL connections on opposite axes) will now defer permanently — vanilla's normal player-placement cascade would set `up=true` upon torch placement, but the queue-time defer never lets the placement happen. If you hit this, placing the torch manually triggers vanilla's normal cascade and works.
- Fix: Old blueprints (scanned before a JSON rule modernisation) now migrate their `requiredBlockOffsets` from current rules when the saved set is empty. Symptom: a blueprint scanned back when `facings.json` still used legacy `minecraft:torch[facing=up]` selectors (which never matched the modern `minecraft:torch` block) saved torches with empty offsets in NBT; those torches' `isReadyToPlace` returned true vacuously (empty `allMatch` → `true`) and queued regardless of whether their support was built, producing a throw→undo→refund→re-queue loop the user observed as "torches floating in the sky on repeat" once the supporting walls couldn't be placed (out of resources). The migration runs in `SchematicBlockDefault.deserializeNBT` only when the saved offset set is empty — a non-empty saved set was written deliberately (potentially with rotation applied) and unconditionally adding un-rotated rule offsets on top would over-defer rotated blueprints. Old broken saves are exactly the empty-set case, so the targeted check fixes them without breaking rotated saves.
- Fix: Torches placed on top of cobblestone walls (and similar "block-below has up-property" setups) no longer cycle in throw→destroy→throw forever. The pre-setBlock `canSurvive` check in `SchematicBlockDefault.build()` evaluated against the *current* world state, which for a freshly-placed wall reads `up=false` (no neighbour above yet) — so canSurvive failed for the torch and the placement deferred forever, even though vanilla's setBlock cascade would have set the wall's `up=true` because of the torch above and the torch would have been fine. Replaced with a post-`setBlock` validation that uses the existing `updateShape` iteration: if `updateShape` returns AIR for any direction (the block self-destructed because vanilla decided it can't survive), the placement is rolled back (including the atomic other-half if it's a bed FOOT or door LOWER) and items are refunded via the existing `cancelPlaceTask` path. This reflects post-cascade reality rather than pre-cascade pessimism, so torch-on-wall and similar "cascade fixes my support" cases now work, while torches in genuinely unsupported positions still get caught (vanilla `updateShape` returns AIR, my iteration undoes). The atomic dual-half placement for beds/doors had to move BEFORE the updateShape iteration so the iteration's other-half-direction call doesn't mistake the missing other half as a self-destruct trigger.
- Fix: Doors now consume only one door item per door (was two), with no half-door placements left in the world if either half is blocked. Same shape as the bed atomic-dual-half fix one entry below: `DoorBlock` instances are detected via instanceof in `addClassBasedRequiredBlockOffsets` (covers all 14+ vanilla wood/iron/copper/nether door variants and any modded doors automatically), the UPPER half returns empty `computeRequiredItems` (so the resource panel shows the right count), `build()` for LOWER pre-checks the UPPER position is clear/replaceable and atomically setBlocks both halves with the LOWER's FACING/HINGE/OPEN/POWERED state copied through to UPPER (only HALF differs). Torches don't need analogous treatment — they're single blocks already covered by `facings.json` (floor torches require block-below; wall torches require the wall behind them) and have been deferring correctly via `requiredBlockOffsets` since the JSON modernization a few entries up.
- Fix: Beds now consume only one bed item per bed (was two), and the place no longer leaves a half-bed in the world if either position is blocked. Vanilla beds are two adjacent half-blocks (FOOT + HEAD) sharing a single bed item; the architect captures the halves separately, which used to mean the Builder consumed two bed items per bed and produced visible half-bed placements. Now: HEAD positions return an empty required-items list (so the resource panel shows the right count, and no extraction happens) and have a `requiredBlockOffset` pointing at the FOOT position (so the HEAD never queues an independent place task — it flips to CORRECT after the FOOT places both halves). The FOOT additionally requires the block below the HEAD position so vanilla `updateShape` doesn't destroy a bed whose HEAD half lacks support, and FOOT's `build()` pre-checks that the HEAD position is empty/replaceable before running setBlock — if not, the placement defers (item refunded) until the HEAD position is cleared. After both checks pass, FOOT's build atomically setBlocks both halves. Class-based detection in `addClassBasedRequiredBlockOffsets` (called from both `setIgnoredProperties` and `deserializeNBT`) means any vanilla or modded BedBlock subclass picks up the linkage automatically — no need to enumerate all 16 colours.
- Fix: Snow layers, carpets (16 colours + moss + pale_moss), and pink_petals now have their `block-below-required` rule entry, so the Builder defers their placement until the supporting block beneath them is built. Without this, the Builder would queue a snow_layer placement at a position with no support, the per-build `canSurvive` defer would catch it (return false → `cancelPlaceTask` refunds the item), the position would re-classify as TO_PLACE next check, the cycle would repeat — wasting a place-task animation cycle and a tick of battery power per attempt until the supporting block happened to be placed. Now those placements simply don't queue until the position-below check result reads CORRECT, matching how torches / saplings / doors / etc. already behave.
- Fix: Builder-placed glass panes, fences, walls, redstone wire, and similar neighbour-aware blocks now actually connect to their neighbours after placement. Vanilla `setBlock` with flag 11 calls `updateShape` on each *neighbour* of the placed block (so they can update their connections to the new block) but doesn't call updateShape on the placed block itself. Combined with the `ignoredProperties` carve-out resetting connection bits to the default no-connection state at build time, that left every Builder-placed pane as a visible single column even when surrounded by valid connectible blocks; breaking and re-placing the pane by hand worked because vanilla's `getStateForPlacement` chain runs updateShape during player placement. `SchematicBlockDefault.build` now iterates each of the six neighbours after `setBlock` and applies `BlockState.updateShape`, mirroring what player placement does. Skipped if updateShape returns AIR (would mean the block decided it can't survive — canSurvive above should have caught that, and removing a freshly placed block is unsafe).
- Fix: Stair blocks (any vanilla or modded `StairBlock` instance) now have their `shape` property auto-added to `ignoredProperties` for placement comparison, regardless of which stair variant. Vanilla has 50+ stair block IDs across wood / stone / brick / nether / deepslate / copper variants; listing them all in `multiple_variants.json` is brittle and doesn't help with modded stairs. Without this carve-out, stairs near other stairs (or near a fluid event that nudged neighbours) cycled in break+replace forever — `SchematicBlockDefault.isBuilt`'s strict comparison saw the schematic-captured shape vs the world-recomputed shape (vanilla updates corner shapes inner_left/outer_right based on actual neighbours) as a mismatch even when the build was semantically complete. New `addClassBasedIgnoredProperties()` helper called from `setIgnoredProperties` (fresh scans) and `deserializeNBT` (old saved schematics get migrated automatically). Same hook can be extended for other shape-aware classes if needed.
- Fix: Builder no longer cycles fragile schematic blocks (snow_layer, carpet, button, redstone wire, sapling, torch, sign, lever — anything where `canBeReplaced(state, water)` returns true) through an extract-then-defer-then-refund loop in REPLACE mode when the build area has fluid 2+ cells away that flows in over a few ticks. The per-build fragile defer in `SchematicBlockDefault.build` only sees immediate-neighbour fluid; for water further out, the place task got queued (extracting items from inventory at queue time), the animation ran, the defer fired in build (because by then the water had flowed close enough to be a neighbour), the items got refunded by `cancelPlaceTask`, and the position re-classified for the next cycle. The user-visible symptom was the inventory slot count for snow layers (etc.) bouncing up and down in real time as the cycle ran. Now the place-task-add filter in `SnapshotBuilder.tick` runs the fragile check at queue time: REPLACE + area has any fluid + schematic block is fragile = don't queue, items stay in inventory. Solid blocks (canBeReplaced=false for water — stone, planks, etc.) are unaffected and proceed to placement normally; the per-build defer remains as a backstop. New `SnapshotBuilder.isFragileSchematicAt(BlockPos)` (default false), overridden in `BlueprintBuilder` to inspect the schematic's `BlockState.canBeReplaced(Fluids.WATER)`.
- Fix: Builder no longer deadlocks in CLEAR mode at positions where the schematic has a non-air block (e.g. snow_layer) and the world has water. Previous classification: `BlueprintBuilder.canPlace` returned true for those positions in REPLACE/CLEAR (the "fluid carve-out" so the place path could waterlog or destroy the source in one pass), routing them into TO_PLACE. The mop gate then deferred them because the area "still had fluid" — but the only fluid keeping the gate closed was the fluid AT THOSE POSITIONS, which couldn't get cleared because they were TO_PLACE not TO_BREAK and the break path doesn't touch TO_PLACE. Symptom: builder ignores the water in front of it forever even after the user sealed the leak. Fix: in CLEAR mode `canPlace` returns false for world-fluid positions, routing them into TO_BREAK. The mop sweeps the fluid; the position re-classifies as TO_PLACE on the next check pass once the world position is empty; the place runs after the rest of the area is also dry. REPLACE mode keeps the existing fluid-carve-out behaviour (place runs immediately and the build path waterlogs / destroys the source in the same pass — that's the cheaper one-pass option REPLACE was designed for, and it doesn't deadlock because REPLACE never closes the mop gate).
- CLEAR fluid-handling-mode spec change: blueprint-specified fluid positions are no longer preserved. The original CLEAR spec ("clear schematic-air-positioned fluids only, preserve blueprint fluids to avoid oscillating on water-feature blueprints") didn't match the user-stated intent of "clear all fluids in the build area" — a blueprint that captured water around solid features (snow trail through a stream, flooded ruin, etc.) would leave every captured-fluid position untouched in CLEAR, defeating the mode entirely. New behaviour: in CLEAR mode `BlueprintBuilder.isAir` treats `SchematicBlockFluid` instances as air for classification purposes, so a position whose blueprint specifies water becomes TO_BREAK when the world has water there and CORRECT when the world is empty (matching the air-treated schematic). Water-feature blueprints can use REPLACE if they want to keep their fluid; CLEAR is now unambiguously "I want this area dry no matter what." Updated the EnumFluidHandlingMode docstring with the new spec.
- Fix: Builder's CLEAR-mode "wait for mop to finish before placing" gate no longer deadlocks when the build area contains waterlogged source-emitting blocks (waterlogged stairs, fences, slabs, etc.). The deadlock: a waterlogged block emits water to its neighbours, so corner positions perpetually re-flood and the Builder's break beams fire forever; meanwhile the place tasks that would dry the source-emitting blocks (and stop the emission) sit deferred behind the gate because "the area still has fluid." Carve-out: waterlog-clear-only place tasks bypass the gate. Drying them is itself part of mopping (turning off the source emission) so they belong in the same phase as the fluid-break tasks, not the post-mop place phase. Implementation: new `SnapshotBuilder.isAllowedDuringFluidMop(BlockPos)` (default false), overridden in `BlueprintBuilder` to return true for positions where the schematic's `isWaterlogClearOnly` matches (CLEAR mode + same block + world wet / schematic dry). The place-task-add filter passes the carve-out through. Other placements (real block placements, fragile blocks at risk of timing-race floodout) still wait for the area to be fluid-free.
- Fix: Builder's CLEAR fluid-handling mode now actually clears water from waterlogged blocks where the schematic captured the block dry. Previously CLEAR matched REPLACE on the place path — both opportunistically waterlogged a placed block over a water source, and `SchematicBlockDefault.isBuilt` had a leniency clause that treated "world wet, schematic dry" as already-built (the original purpose was to prevent REPLACE-mode placements from looping break+place when REPLACE itself produced the world-wet state). The combination meant a wet fence whose schematic counterpart was dry was permanently considered built and the user-stated CLEAR intent ("clear all fluids") didn't apply to waterlog-style fluid. New behaviour: REPLACE keeps the opportunistic waterlog (preserve water "for free"); CLEAR respects the schematic's WATERLOGGED value strictly. Implementation: `SchematicBlockDefault.isBuilt` got a fluidMode-aware overload that disables the leniency under CLEAR, `BlueprintBuilder.isBlockCorrect` now passes the mode through, and `SchematicBlockDefault.build` differentiates the two modes when deciding whether to set WATERLOGGED on the placed state. The architect's table already captures WATERLOGGED (it's just a property on the BlockState round-tripped through `NbtUtils.writeBlockState`), so nothing changes on the scan side. To avoid wasting items when "drying" a block whose only difference from the schematic is its WATERLOGGED property, the place path detects this case (`SchematicBlockDefault.isWaterlogClearOnly`) and skips item extraction entirely — the position gets re-set with WATERLOGGED=false, no fence is consumed from inventory. Six new game tests under `buildcraftunofficial:fluidmode_clear_dries_*` / `_replace_still_opportunistically_*` / `_isbuilt_strict_in_clear` / `_isbuilt_lenient_in_replace` / `_clear_only_under_clear` / `_clear_only_requires_matching_block` cover both directions of the mode split + the helper's predicate.
- Fix: Builder break-task ordering in CLEAR mode now goes source-fluid first, then flowing-fluid, then non-fluid blocks (within each tier the existing distance-from-centre order is preserved). For mixed source+flowing fluid bodies, breaking sources first lets the remaining flowing drain naturally, which (a) cuts the total break count needed for self-contained pools, and (b) handles MC's infinite-water regeneration mechanic better — when a 2×2 pool of sources gets multiple sources broken in the same tick (now likely with the 1 MJ fluid cost from a few changes up), no remaining source has 2+ source-neighbours so the empty cell can't re-source. Doesn't help leaky-boundary cases (sources outside the build area), but the prior "infinite mop" CLEAR-mode behaviour remains the right answer there: the activity is visible so the user can extend the build area / fix the source / switch to REPLACE.
- Fix: Builder/Quarry/Filler fluid break cost is no longer absurd. Vanilla pure-fluid blocks (`Blocks.WATER`, `Blocks.LAVA`, modded LiquidBlock instances — BC oil, fuel) ship with `strength(100.0F)` — that's their *explosion resistance*, not break time, but `BlockUtil.computeBlockBreakPower`'s `(hardness+1)*2 * 16 MJ` formula reads it as time-to-break and produced 3232 MJ per fluid block (40× stone, more than obsidian). At MAX_POWER_PER_TICK = 256 MJ/tick that's ~13 ticks per single water break — the user-visible "laser fires forever, water never clears" symptom that survived both the BlockUtil setBlock-AIR fix and the battery-sync fix. Now LiquidBlock instances charge a flat 1 MJ each (matching vanilla's "free to break with empty hand" intent); non-fluid blocks keep the original formula. Three new game tests `buildcraftunofficial:blockutil_{water,lava,stone}_break_cost*` cover both directions of the carve-out.
- Fix: Builder's CLEAR fluid-handling mode now defers all place tasks until every fluid in the build area has been mopped — no more "snow placed adjacent to water corner, water flows back next tick, snow drops as item entity nobody picks up, place-task re-queues, repeat" wasted-resource loop. The fragile-block defer in `SchematicBlockDefault.build()` (added a few commits up) is still there as a backstop for REPLACE mode and other narrow cases, but it can only see immediate-neighbour fluid at the moment of placement; water flowing in from one cell over within the same or next tick lands on the freshly placed block and destroys it before the defer fires next cycle. CLEAR-mode users said "logically that should be the order of operations" (clear all fluids, then build), so `SnapshotBuilder.tick`'s place-task-add gate now also requires `!buildAreaHasAnyFluid()` under CLEAR. Trade-off: at a leaky boundary (fluid source outside the build area) the Builder will mop forever and never start placing — but the activity is visible (break beams keep firing) so the user can extend the area / fix the leak / switch to REPLACE rather than wonder why nothing's happening. Added `buildAreaHasAnyFluid()` helper to `SnapshotBuilder` (O(n) box scan, short-circuits on first fluid found, only called once per tick during the place-task-add gate so the cost is negligible).
- Fix: `BlockUtil.breakBlockAndGetDrops` now actually clears pure-fluid blocks instead of silently no-op-ing them. The previous `world.destroyBlock(pos, false)` call delegates internally to `setBlock(pos, fluidState.createLegacyBlock(), 3)` — for a position whose only "block" IS the fluid (water, lava, oil, fuel — any `LiquidBlock` instance), `createLegacyBlock()` returns the same fluid state, so destroyBlock is a silent no-op while still reporting success. The Builder's CLEAR-mode break tasks were "succeeding" against water (returning `Optional.of(emptyList)`, removing the task from the queue) but the water never actually disappeared; the position re-classified as TO_BREAK on the next check, re-queued, fired the laser again, looped forever — the user-visible "phantom laser fires at water source, water never gets mopped" symptom that survived the battery-sync fix two commits up. Now LiquidBlock positions take a hard `setBlock(pos, AIR, 3)` (which clears both the BlockState and the derived FluidState); non-fluid blocks keep going through `destroyBlock` so its sound / particle / level-event side effects still fire; waterlogged blocks (block != LiquidBlock, fluid via WATERLOGGED) keep the destroyBlock + legacy-fluid-restore behaviour, which is correct there because we're breaking the block, not the fluid (the fluid clearing happens on a second break pass once the block is gone). Three new game tests under `buildcraftunofficial:blockutil_break_*` cover water source clear, flowing-water clear, and the regression case for solid blocks (stone still drops cobblestone and the position becomes air via destroyBlock). Side effect of this fix: the Builder's CLEAR-mode "perpetual mopping" at a leaky boundary will now visibly progress (each laser cycle removes the flowing fluid for ~1 fluid tick before it re-floods); previously the visual was the same but no actual work was happening. The block-throwing place animation may appear choppy while CLEAR is mopping fluid because client-side place-task extrapolation is gated on the break queue being empty — that's by-design (server prioritises break over place, and naively extrapolating place ahead of server placement would visually desync the throw landing) and resolves once the area is dry.
- Fix: Builder/Filler client/server battery sync no longer accumulates past capacity. `MjBattery.addPower(long, boolean)` is unbounded — a bare `microJoules += n` with no clamp — and the additive semantics are correct for external energy receivers (engines, MjBatteryEnergyHandler) but were also being used by `TileBuilder.loadAdditional` / `TileFiller.loadAdditional` to apply the synced battery snapshot from `ClientboundBlockEntityDataPacket`. Since that packet re-runs `loadAdditional` every 5 ticks, every sync was *adding* the server's value into the existing client value instead of replacing it. After ~13 seconds the client battery climbed to ~65× capacity (reported in the wild as 1,036,410 / 16,000 MJ on a Builder whose server-side battery sat at 0). The downstream symptom was phantom laser firing in CLEAR mode: `SnapshotBuilder.clientTick` gates animation extrapolation on `stored > 0`, so the inflated client battery let it locally charge break tasks past their target, drawing a laser at every queued source — while the server, with 0 MJ, never actually progressed any task and the water never broke. Added an absolute `MjBattery.setStored(long)` (clamps to `[0, capacity]`) and switched both load paths to it; `MjBattery.deserializeNBT` and `readFromBuffer` also route through it for consistency. The Filler's pre-existing `extractAll() + addPower()` workaround (which produced the right value but bypassed the symptom rather than fixing the cause) is replaced. Five new unit tests under `MjBatteryTest` cover the absolute-set semantics, the `[0, capacity]` clamp, the no-accumulation invariant on repeated calls, and the additive-`addPower` behaviour stays untouched. After this commit the user's "no engine, server battery 0, but client shows millions of MJ and lasers fire at nothing" scenario reads as `0/16000` on both sides, no laser; once power is connected the value rises in lockstep and the laser fires only when there's real power for a break task.
- Tuning: oil generation in oceans is now tiered, and overall ocean oil density is substantially reduced. The 26.1 default `surfaceDepositBiomes` had merged every vanilla ocean variant (shallow + deep) plus deserts/badlands into one tier with a flat 3.0× spawn-rate bonus AND eligibility for the dominant `LAKE` roll (the 6%-per-chunk surface-tendril type), giving any ocean ≈6.4% oil per chunk — about 45× the 1.12.2 vanilla-ocean rate, and the reason oil felt "guaranteed around any large body of water". `OilGenerator` now reads two separate biome lists: the existing `surfaceDepositBiomes` (light tier, 1.25× bonus, no LAKE roll) defaulting to shallow ocean variants only (`ocean`, `warm_ocean`, `lukewarm_ocean`, `cold_ocean`, `frozen_ocean` — `frozen_ocean` was missing from the prior defaults), and a new `richSurfaceDepositBiomes` (rich tier, 1.5× bonus, LAKE roll allowed) defaulting to deep ocean variants plus the existing land oil biomes (`deep_ocean`, `deep_lukewarm_ocean`, `deep_cold_ocean`, `deep_frozen_ocean`, `desert`, `badlands`, `eroded_badlands`, `wooded_badlands`). The `smallOilGenProb` default has also been quartered from 2.0% to 0.5% — this is the rate that drives the dominant LAKE-style surface tendrils, which had been flooding ocean horizons. Resulting per-chunk probabilities: plain land 0.14% (unchanged), shallow ocean ≈0.18% (1 in 572, a "slightly higher" tier), deep ocean ≈0.95% (1 in 106 — clearly the prize, but a finite-search rather than an everywhere-you-look prize), average ocean ≈0.43% (about 15× rarer than before). Behavioural change in the algorithm is one-line: the `LAKE` gate switched from `oilBiome` (light-tier-or-better) to `richBiome` (rich-tier-only), so the giant 6-block-radius surface tendrils with 25-45-block branch radii now appear only in deep oceans and on land deposit biomes rather than blanketing every coastal shallow. The `excessiveBiomes` 30× knob and `oilWellGenerationRate` global multiplier still apply on top; `mediumOilGenProb` and `largeOilGenProb` are unchanged. **Existing worlds**: TOML config files preserve their stored `smallOilGenProb` value once written, so to pick up the new 0.5% default delete `config/buildcraftunofficial-worldgen.toml` (or hand-edit the `smallOilGenProb` line) and let the mod regenerate it. New lang entry `buildcraftunofficial.configuration.richSurfaceDepositBiomes` for the config UI. The custom 1.12.2-era `BiomeOilOcean` / `BiomeOilDesert` biomes and their Simplex-noise injection layers remain unported — this tier system reproduces the "deep oceans are the oily ones" feel without re-introducing custom biomes.
- Fix: REPLACE/CLEAR fluid-handling no longer loops place→destroy→place when a "fragile" block (snow_layer, carpet, button, redstone wire, torch, sapling, sign, lever, pressure plate — anything whose `BlockBehaviour.canBeReplaced(state, fluid)` returns true) is being placed adjacent to fluid. The earlier behaviour: the REPLACE path destroyed the source at the target position and placed the block; on the next fluid tick, water from a horizontal/up neighbour (or from another source bordering the broken position) flowed back, displacing the freshly placed block; the Builder consumed one item per cycle until the inventory drained. `SchematicBlockDefault.build()` now defers placement (returns false → existing `cancelPlaceTask` refunds the item) when the resulting block is fragile (canBeReplaced returns true for the neighbour's fluid type), the block isn't being waterlogged, and any of the four horizontal neighbours OR the block above holds fluid. Solid blocks (canBeReplaced=false) and waterlogged blocks (coexist with their fluid) are exempt. Bonus structural fix: the existing source-destroy was previously fired *before* the canSurvive check, so a failing canSurvive defer would leave the world torn down with no replacement; world mutations are now deferred until every "should we abort?" check has passed.
- Fix: Builder's CLEAR fluid-handling mode now actually mops up flowing fluid, not just sources. Previous behaviour — `SnapshotBuilder.shouldBreakQueueAcceptFluid` only let sources through and assumed flowing fluid would drain naturally once every contributing source was inside the build area. That assumption fails at a leaky boundary: a build area at the edge of a pool, river, or any body whose source extends outside. There, broken positions immediately refilled with flowing fluid from outside; classified TO_BREAK, filtered out by the source-only rule, no break beam ever fired again, and the Builder appeared stuck after one or two beams. CLEAR now accepts both sources and flowing fluid through the break queue, so the Builder keeps mopping until either the area is dry (whole pool inside) or the user stops it / extends the area / deals with the external source (leaky boundary). The activity is visible — beams keep firing — so the user sees what's happening rather than wondering why nothing is. Six new game tests under `buildcraftunofficial:fluidmode_fragile_*` / `_solid_*` / `_waterloggable_*` / `_no_replace_skips_*` cover the defer's positive path, the don't-defer carve-outs (solid block, waterloggable block, NO_REPLACE mode), the UP-neighbour case, and the world-untouched invariant on defer.
- Engine power-ledger icon animations are now resource-pack overridable. `LedgerEngine.drawIcon()` and `LedgerEngineFE.drawIcon()` had been animating the active/warm engine icons in Java — manual `vOffset = (System.currentTimeMillis()/50 % 6) * 16f` against the 16×96 sprite sheet, hardcoded to 6 frames at 1 tick each. Replaced with a sprite-atlas lookup via the existing `SpriteHolderRegistry` (the engine icons live under `buildcraftunofficial:textures/icons/`, already stitched into the blocks atlas via `atlases/blocks.json`'s `icons/` directory source). `graphics.blitSprite(RenderPipelines.GUI_TEXTURED, sprite, x, y, 16, 16)` reads the atlas sprite's current-frame UVs each call, so animated entries auto-advance through Minecraft's atlas-animation ticker. The `engine_{active,warm}.png.mcmeta` sidecars finally become load-bearing — drop a resource pack with a different `frametime`, custom `frames` array, or `interpolate: true` and the change applies without touching code. `engine_inactive` and `engine_overheat` get the same code path (no animation, just static atlas sprites) so the rendering branch is uniform.
- Correction to the Step 5+6 changelog entry below: the "fix latent rendering bugs" framing for the `.mcmeta` and `buttons.png` moves was wrong. On closer inspection: (a) `LedgerEngine.drawIcon()` and `LedgerEngineFE.drawIcon()` already animate the engine-active and engine-warm icons via Java time-based UV cycling (`vOffset = (System.currentTimeMillis()/50 % 6) * 16f` against a 16×96 sheet) — they bypass Minecraft's animation system entirely, so the legacy `.mcmeta` sidecars (containing the standard `{"animation":{"frametime":1}}`) had been inert all along, both before and after the move; (b) `misc_slots.png.mcmeta` contains a custom `buildcraft_data` sprite-atlas block, but a grep of the codebase for `buildcraft_data` / `getMetadataSection` / `loadMetaSection` returns zero hits, so nothing reads it; (c) `StandardButtonTextureSets.BUTTON_TEXTURES` is referenced only by `ButtonTextureSet`'s default-texture constructor, and `new ButtonTextureSet(...)` doesn't appear anywhere in the codebase — the entire BC custom-button path is dead code that the project migrated away from in the recent vanilla-`widget/button` switchover. So Step 6 was a pure namespace cleanup, not a bug fix. Apologies — I'll be more careful when claiming "this was silently broken" without verifying the actual rendering path. The follow-up commit refactors `LedgerEngine`/`LedgerEngineFE` to actually use Minecraft's animation system (via the existing `SpriteHolderRegistry`/blocks atlas), which will let resource packs override `frametime`, frame order, and interpolation.
- Cleanup (Steps 7+8 of the legacy-namespace plan, the final bulk pass): `git rm -r` of every remaining file under `assets/buildcraftbuilders/`, `assets/buildcraftcore/`, `assets/buildcraftenergy/`, `assets/buildcraftfactory/`, `assets/buildcraftrobotics/`, `assets/buildcrafttransport/`, and `assets/buildcraftlib/` (250 files). Each file was either at the wrong subdirectory for its kind (modern Minecraft moved recipes/advancements/loot from `assets/<ns>/recipes,advancements,loot_tables/` to `data/<ns>/recipe,advancement,loot_table/`), in a 1.12.2 forge format that the modern recipe/advancement loaders can't parse anyway, or — for the `buildcraftlib/textures/` stragglers (`help_split.png`, `loading.png{,.mcmeta}`, `warning_{major,minor}.png`, `debugger.png`, `guide_note.png`, `trunk_creative.png`, `trunk_overheat.png`) — verified by grep to have zero callers (the lone `help_split.png` mention was a Java comment; `trunk_overheat` is aliased to `trunk_black` in the active engine models). The two `.lang` reference files (960 + 35 lines of robotics / guide-chapter translations) were deleted too — `.lang` format hasn't been read since 1.13, the strings live in git history and the `8.0.x-1.12.2` branch for whoever ports the robotics module later. After this commit, `assets/` contains only `buildcraftunofficial/` and the vanilla-merge `minecraft/`. Verified all 55 game tests still pass and `compileJava` is clean. The full audit and step-by-step results are recorded in [docs/legacy-namespace-audit.md](docs/legacy-namespace-audit.md).
- Cleanup (Steps 5+6 of the legacy-namespace plan) + two latent rendering bugs fixed along the way: moved 6 actively-referenced GUI textures from `assets/buildcraftlib/textures/` to `assets/buildcraftunofficial/textures/` (`git mv` so history is preserved): `gui/buttons.png`, `gui/misc_slots.png.mcmeta`, `icons/engine_active.png.mcmeta`, `icons/engine_warm.png.mcmeta`, `icons/lock.png`, `items/guide_book.png`. Two of those moves fix bugs that have been silently shipping since the 26.1 port: `LedgerEngine`/`LedgerEngineFE` ask the resource manager for `buildcraftunofficial:textures/icons/engine_{active,warm}.png`, and Minecraft loads `.mcmeta` sidecars from the same namespace as the PNG — so the canonical PNGs were rendering as static frames because their animation metadata was sitting in the unregistered `buildcraftlib` namespace. Same shape for `StandardButtonTextureSets`/`GuiAutoCraftItems`/`GuiElementStatement` looking up `buildcraftunofficial:textures/gui/buttons.png` (PNG only existed in legacy) and `misc_slots.png` (the PNG was canonical, the `.mcmeta` was legacy). After the move the engine-active and engine-warm icons actually animate, and the button/slot atlases load against their full intended sources. Updated the remaining `buildcraftlib:` references: `GuiFiller.java:195` (the inverted-mode lock indicator) and `marker_path.md:17` (a guide-page image). Also fixed three broken legacy-namespace `<image src>` references in the guide markdown that pointed at textures whose namespaces had no textures at all (`buildcraftcore:textures/items/marker_path.png` → canonical, `buildcraftenergy:textures/gui/combustion_engine_gui.png` → canonical, `buildcraftcore:items/wrench` → canonical in two places in `guide_page_format.md`). After this commit, no Java/JSON/markdown reference to `buildcraftlib:` remains in the codebase outside the changelog and audit doc.
- Cleanup (Steps 3+4 of the legacy-namespace plan): deleted two unused legacy script files — `data/buildcraftcore/compat/buildcraft/book.txt` and `data/buildcraftlib/compat/buildcraft/book.txt`. They were registering separate guide books (`buildcraftcore:main` and `buildcraftlib:meta`) under dead namespaces; nothing in Java references either id, no `guide.txt` populates them with entries, and `ScriptableRegistry` walks every `data/<ns>/` subfolder so they were silently being added to `GuideBookRegistry` on every reload as registered-but-empty noise. (Pre-existing follow-up flagged in the audit doc: `BCCoreCreativeTabs.java:43` calls `ItemGuide.createForBook(..., "buildcraftunofficial:config")` but no script defines that book — separate from this cleanup.) Also fixed `CraftingUtil`'s `@EventBusSubscriber(modid = "buildcraftlib")` to `"buildcraftunofficial"` so its `onServerStarted` debug handler actually registers — the modid argument has to be a registered modid for FML to wire the handler to the mod's event-bus context, and `buildcraftlib` is not registered.
- Cleanup (Step 2 of the legacy-namespace plan): moved the 32 builder compat rule JSONs from `assets/buildcraftbuilders/compat/buildcraft/builders/` to `assets/buildcraftunofficial/compat/buildcraft/builders/` (`git mv` so blame/history is preserved), and reverted `RulesLoader.loadAll()` to its plain `for (modInfo : ModList.get().getMods())` loop — the `buildcraftbuilders` fallback set added two changes ago is no longer needed because the canonical iteration now finds the files at the registered namespace. `READ_DOMAINS.add("buildcraftunofficial")` then guarantees the canonical scan happens regardless of mod ordering. Verified all 55 game tests still pass: torch/wall/leaves/fence rule-driven placement, fluid handling mode, support-required, etc. all continue to read the same rule data, just from the renamed path.
- Audit + cleanup: catalogued the 8 legacy `assets/` namespaces (and 2 legacy `data/` namespaces) carried over from the 1.12.2 multi-mod suite — only `buildcraftunofficial` is registered, so anything reached via `ModList.get().getMods()` iteration silently misses these. Wrote up the inventory, the active-vs-orphan classification, and a phased migration plan in [docs/legacy-namespace-audit.md](docs/legacy-namespace-audit.md). Found one more silent-bug case in the same shape as `RulesLoader`: `CraftingUtil` is annotated `@EventBusSubscriber(modid = "buildcraftlib")` and so its server-started subscriber never registers (low impact — the handler is just a debug recipe-class dump). Executed the obviously-safe deletes from Step 1 of the plan: `assets/buildcraft/logo.png` (duplicate of the active root `logo.png`), `assets/buildcraftlib/atlases/blocks.json` + `textures/block/white.png` (orphan atlas referencing only itself), `assets/buildcraftcore/recipes/list.json.old.txt` (explicit `.old`), and `assets/buildcrafttransport/recipes/_constants.json` + `_factories.json` (1.12.2 forge-recipe metadata; `_factories.json` references `buildcraft.transport.recipe.EngineRegisteredFactory`, a class that no longer exists). Larger moves (RulesLoader compat tree, lib texture references, guide markdown image fixes) are left as separate commits per the plan.
- Fix: `RulesLoader.loadAll()` now also scans the legacy `buildcraftbuilders` resource namespace, not just the registered modid namespaces. The vanilla compat rule files (`assets/buildcraftbuilders/compat/buildcraft/builders/*.json` — torches, walls, leaves, doors, fluid handling, fence/pane connection ignores, redstone-cycling carve-outs, etc.) live under that namespace as a holdover from 1.12.2 when BuildCraft was a multi-mod suite (`buildcraftcore`, `buildcraftbuilders`, …). The 26.1.x port merged the source into a single `buildcraftunofficial` mod but left the resource files at their historical paths; since `ModList.get()` never yields a mod named `buildcraftbuilders`, the entire rule set was silently failing to load — every block ended up with empty `requiredBlockOffsets`/`ignoredProperties`/`placeBlock` and the Builder lost dependency-aware placement ordering plus the "ignore neighbour-cycling properties" carve-outs. (This also explains why every previous "modernized the JSON rules" entry above this one was a no-op until now — those files were correct, just never read. The torch fix landed earlier still works because it ran on the canSurvive side, independent of rules.) Adding `buildcraftbuilders` to the scanned namespace list in `loadAll()` is the minimal fix, leaving everyone else's blueprint resource paths untouched.
- Fix: `SchematicBlockDefault.deserializeNBT` now re-derives `ignoredProperties` from the current JSON rules instead of trusting only the saved values. Old blueprints (scanned before a rule was added — e.g. before `multiple_variants.json` got the wall connection / leaves persistent carve-outs) had empty `ignoredProperties` baked into their NBT, and `init()` is only called at scan time, not at load time, so without this migration the user would have to re-scan every blueprint after every rule update. The migration is additive (saved entries are kept, new ones from rules are added by name) so user-customised rule files won't have entries silently dropped. Two new game tests `buildcraftunofficial:builder_deserialize_migrates_{wall,leaves}_ignored_properties` cover the load path.
- Fix: Builder no longer loops break→place forever on leaves and on neighbour-aware blocks (cobblestone walls and the rest of the WallBlock family, every wood/nether-brick fence, iron bars, glass panes, all 16 stained glass panes). Two distinct loops:
  - Leaves: schematics captured natural foliage with `persistent=false`. Once the Builder placed them away from a log, vanilla's randomTick pushed `distance` to 7, `decaying()` fired, and the leaves dropped to air. `isBuilt` then said world≠schematic and re-queued the position; the next placement decayed again. `SchematicBlockDefault.build()` now forces `PERSISTENT=true` on any LeavesBlock state right before `setBlock` (mirrors what vanilla `LeavesBlock.getStateForPlacement` does for player-placed leaves).
  - Connection-aware blocks: the schematic captured the `north/east/south/west`(/`up` for walls) connection state from the source world; after Builder placement, vanilla `updateShape` rewrote those values to match the new neighbours. `isBuilt`'s strict equality saw the diff and marked the position TO_BREAK. Added `multiple_variants.json` entries that put `north/east/south/west(/up)` into `ignoredProperties` for all walls, fences, iron bars, and glass panes — same approach as the existing `lit`/`powered` carve-outs for redstone state cycling. Connections still end up correct in-world because `updateShape` keeps maintaining them; we just stop re-comparing them at `isBuilt` time. Redstone wire is intentionally NOT included — its connection enum encodes player intent (visual run direction) so the strict-equality semantics remain correct.
  - Three new game tests `buildcraftunofficial:builder_leaves_placed_persistent`, `builder_wall_isbuilt_ignores_connections`, `builder_fence_isbuilt_ignores_connections` cover both fixes.
- Modernized the Builder's vanilla compat rule files (`assets/buildcraftbuilders/compat/buildcraft/builders/vanilla/{facings,block_below_required,multiple_variants}.json`) so dependency-aware placement ordering finally applies to torches, signs, banners, doors, levers, flowers, saplings, beds, rails, hanging-from-wall blocks, and the redstone-state cycling blocks. The legacy files used 1.12.2 flat-block IDs (e.g. `minecraft:torch[facing=north]`, `minecraft:wooden_door`, `minecraft:tallgrass`, `minecraft:reeds`, `minecraft:wall_sign`, `minecraft:standing_banner`, `minecraft:powered_repeater` …) that don't match modern split blocks (`wall_torch`, per-wood doors, `short_grass`/`fern`/`tall_grass`, `sugar_cane`, per-wood `*_wall_sign`, per-color `*_wall_banner`, `repeater[powered]`, `redstone_torch[lit]`, `furnace[lit]`, …), so `RulesLoader` had been silently producing empty rule sets for all of them. With proper `requiredBlockOffsets`, `BlueprintBuilder.isReadyToPlace` now defers a torch/sign/door/etc. until its support is built, eliminating the wasted animation cycle the canSurvive guard had to absorb. The `multiple_variants.json` entries also pivoted from the legacy "two block IDs that share a place item" pattern to modern "one block ID, ignore the cycling property" — preventing redstone torches, repeaters, comparators, redstone lamps, redstone ore, furnaces, blast furnaces, smokers, and campfires from re-entering break+place loops every time their `lit`/`powered` state cycles.
- Fix: Builder no longer silently consumes torches, ladders, signs, levers, redstone wire, doors, crops, carpets — any block whose `canSurvive()` requires a neighbouring support — when those supports aren't yet in place. Vanilla `Level.setBlock` doesn't validate `canSurvive`, so an unsupported block was set in the world for a tick, popped off by the immediate neighbour-update tick, but `SchematicBlockDefault.build()` had already returned true so `BlueprintBuilder.cancelPlaceTask` never refunded the item. `build()` now rejects placements whose final blockstate (after the REPLACE/CLEAR waterlog toggle, if any) can't survive at the target position; the items are refunded and the position is retried automatically once its support is built. New game tests `buildcraftunofficial:builder_support_*` cover floor torches, wall torches with/without support, ladders without a wall, the no-regression case for unanchored fences, and the sea-pickle-over-water canSurvive-after-waterlog ordering.
- Builder: added a fluid-handling mode button (top-right of the GUI, above the resource grid). Cycles through three states on click — "Do not replace fluids" (🚫, the legacy skip-fluid behavior and new default), "Replace fluids with placed blocks" (bricks icon, waterlogs the placed block if it supports vanilla `WATERLOGGED` otherwise destroys the fluid source), and "Clear out ALL fluid sources" (bucket icon, same as Replace plus destroys fluid sources at schematic-air positions so entire ponds/lava lakes drain). Blueprint-specified fluids are preserved under Clear so water-feature blueprints don't oscillate. Mode persists via NBT, syncs over ContainerData, and invalidates check results on change so flip-flipping a live Builder picks up the new behavior on the next tick. The button uses the vanilla Minecraft `widget/button` 9-sliced sprite via `AbstractButton.extractDefaultSprite`, so its bevel + hover state match the Replacer GUI's Replace button (and any other vanilla Button in the mod) and respect resource-pack overrides for free. New game tests `buildcraftunofficial:fluidmode_*` cover the waterlog/destroy branches, the lava-isn't-water edge case, flowing-vs-source handling, and the enum cycle/ordinal-clamp invariants.
- Fix: Builder no longer break/places waterlogged blocks every check tick (wasting items and power). Under REPLACE/CLEAR the freshly placed block ends up with `WATERLOGGED=true` over a water source, but the architect captured it dry, so `SchematicBlockDefault.isBuilt` saw `world.WATERLOGGED=true ≠ schematic.WATERLOGGED=false` and re-marked the position TO_BREAK forever. `isBuilt` now treats world-wet vs schematic-dry as built (not the reverse — a genuinely waterlogged schematic still demands a waterlogged world). Two new `fluidmode_isbuilt_*` regression tests cover both directions.
- Fix: Blueprint 3D preview (anywhere it appears — blueprint/schematic tooltips, Architect GUI, Replacer GUI) now rotates around the structure's true center. Vanilla's `ItemTransform.NO_TRANSFORM.apply` implicitly translates items by `(-0.5, -0.5, -0.5)` so submitted items render centered on the pose origin; the PiP renderer was translating each cell by `(x, y, z)` on the assumption that items render from their BSW corner. The resulting half-cell offset was invisible in larger blueprints but made single-block schematic previews rotate around a corner. Per-cell translate is now `(x + 0.5, y + 0.5, z + 0.5)` to compensate.
- Replacer block: full port + bugfix + UX rework. The 1.12.2 Replacer shipped unfinished — its palette match used whole-object `.equals()` so freshly captured single-block schematics never lined up with Architect-produced palette entries (the blueprint stayed unchanged while the schematics got consumed), and its rename text box was wired to a commented-out network send. Reworked to a full GUI with: a 3D preview showing the hypothetical post-replace blueprint as you feed in schematics; an editable name field pre-populated from the current blueprint (only committed to the new blueprint when you click Replace); a live "N × Block A → Block B" summary under the slots; a "Replace" button (vanilla-textured, disabled when inputs are incomplete) replacing the auto-trigger-on-tick; Help + Owner ledgers; and `BlockReplacer.setPlacedBy` now records the placing player so the Owner ledger actually fills in. Single-block schematics are no longer consumed — the same from/to pair can be reused on a different blueprint. `Blueprint.replace` now matches on `blockState + placeBlock` rather than full object equality, plus adds `countMatchingPaletteEntries` / `countMatchingCells` helpers for the summary. Single-block schematic items now show the captured block's name in their tooltip and get a 3D preview panel below the main tooltip (via `SchematicSingleTooltipOverlay`, parallel to the existing blueprint tooltip handler). Added game tests under `buildcraftunofficial:blueprint_replace_*` covering the scan-context match regression, multi-occurrence replace, data[] invariance, no-match, counting, and null-safe no-op.
- Electronic Library GUI polish: the progress indicators now reveal the pre-baked filled-arrow sprites on the GUI texture sheet (at UV 234,224 for → and 234,240 for ←) instead of drawing coloured rectangles, so you see an actual arrow "lighting up" rather than a bar — top row (←) reveals right→left, bottom row (→) reveals left→right, matching each arrow's pointing direction. The Delete button has been replaced with Minecraft's native `Button` widget (textured, hover-highlighted) at the original 1.12.2 position (174, 109); it auto-disables when no snapshot is selected. Added a `setPlacedBy` override to `BlockElectronicLibrary` plus an `onPlacedBy` override in the tile that calls `setChanged()` + `level.sendBlockUpdated()` so the placer's `GameProfile` reaches watching clients immediately — the Owner ledger now fills in correctly on freshly-placed blocks. The block's name is centered at the top of the GUI, matching the 1.12.2 layout. Restored the Help ledger (left) and Owner ledger (right) by wiring them up in `initGuiElements`; the Help ledger's per-element text is a stub for now and will be populated in a later session.
- Electronic Library: ported full GUI and logic from 1.12.2. The block now has a working GUI with a snapshot list, selection highlighting, download progress (server library → client library) and upload progress (client library → server library). Snapshot data is transferred over the network in chunks; item slots persist across chunk unloads.
- Fix: Repaired the three game tests that were failing after the previous changelog entry restored visibility. (1) `gui_test_dynamo_upgrade` — its spec referenced `buildcraftunofficial:iron_gear`, but the item is registered as `gear_iron`, so the `.orElseThrow()` on the item-registry lookup threw `NoSuchElementException: No value present` before the test even opened the menu; spec now uses the correct id. Its shift-click `slot` was also pointing at an empty main-inventory slot (36) instead of the hotbar slot (31) where `set_hotbar` had placed the gear, and `TileDynamoMJ` didn't implement `MenuProvider`, so the test's `instanceof MenuProvider` check at menu-open time silently no-op'd — fixed both. (2) `gui_test_filler_ui` had the same 36-vs-54 slot-index bug, plus the final assertion checked for pattern tag `buildcraftunofficial:fill` when `Pattern`'s tag format is `buildcraft:<name>` (and the spec was sending `buildcraft:cylinder`, which isn't a real pattern, so `FillerType.readFromBuffer` fell back to `PATTERN_NONE`). (3) `oil_bobbing_physics` — the oil buoyancy logic was living in `BCCustomFluidType.move(...)`, but `FluidType.move` is a dead override point in 1.21.x (its vanilla default just returns `false`; nothing in patched `Entity`/`LivingEntity` calls it), so our pig-float code never ran and pigs stayed glued to the floor. Moved buoyancy onto a new `BCBuoyantLiquidBlock extends LiquidBlock` via the real `entityInside` hook; all BC fluids now route through it.
- Fix: Restored 15 previously-invisible game tests to the `runGameTestServer` run. In 1.21.11+, a test needs both a `TEST_FUNCTION` (Java) registration and a `test_instance` datapack JSON; the server enumerates the datapack registry, so functions without a matching JSON were silently skipped (only 19 of 33 were running). Added `test_instance` JSONs for the missing 15 (fluid physics, filtered-buffer drops, dynamo/FE-engine upgrade + GUI, filler filter, slot max-stack, builder/architect drops, GUI spec tests).
- Fix: Fixed a server crash in `BlueprintBuilder.cancelPlaceTask` when a fluid place task was cancelled after the fluid-placement deadlock fix enabled them to actually queue. The extract path's "pack fluid info into the resulting bucket item's CustomData" step had been TODO-stubbed to write an empty `BuilderFluidStack` tag, and the corresponding cancel-path `map(stack -> return FluidStack.EMPTY)` then fed `FluidResource.EMPTY` into `tankManager.insert`, tripping NeoForge's `checkNonEmptyNonNegative` precondition with `IllegalArgumentException: Expected resource to be non-empty: minecraft:empty [0]`. Now the extract path actually serialises the fluid id + amount into the bucket's CustomData, and cancel reads it back out to reconstitute the FluidStack for the tank refund — with a defensive filter so any empty/unresolvable fluid (old save data, unregistered fluid) is skipped instead of crashing.
- Fix: Builder now actually places source fluid blocks (water, lava, oil, fuel, …) that appear in a blueprint. Each `SchematicBlockFluid` listed its four horizontal neighbours + the block below in `getRequiredBlockOffsets()`, so `BlueprintBuilder.isReadyToPlace` made every planned source fluid wait for every adjacent planned source fluid to already be `CHECK_RESULT_CORRECT` — for any blueprint where two sources sat next to each other (i.e. every pool) they deadlocked each other and nothing ever placed. `isReadyToPlace` now skips the fluid-to-fluid constraint specifically: source blocks don't need each other placed first (the flowing overspill one creates in the other's cell gets overwritten the moment that cell's source places). Fluid-vs-solid constraints (wait-for-floor, wait-for-walls) are preserved.
- Fix: Corrected block loot-table data path from `loot_table/block/` (singular) to `loot_table/blocks/` (plural) so vanilla's datapack loader actually finds them. Without this the Builder and Architect Table had the loot-table JSONs sitting in an unread directory, so breaking either block in survival with a pickaxe dropped the tile contents but not the block item itself. Same typo was silently shipping for the `filtered_buffer_drops` game test (which was hidden until the `test_instance` gate was fixed) — switched that tester to the survival-player + wooden-pickaxe mining flow so it actually exercises the drop path. Also bypassed the `(ServerPlayer) helper.makeMockPlayer(...)` ClassCastException in `GuiTester.open_menu` by instantiating the block's menu directly from its MenuProvider, and implemented the long-commented-out `click_slot` action via `AbstractContainerMenu.quickMoveStack` for the shift-click case the existing specs use.
- Fix: Breaking the Builder AND the Architect Table now drops the block itself (via new loot tables) plus all of their contents. Builder drops: snapshot slot item, every non-empty resource slot (27 total), and each of the 4 fluid tanks as fluid-shard items. Architect drops: snapshot-in and snapshot-out slots. Matches the 1.12.2 behaviour where `BlockBCTile_Neptune.breakBlock` delegated to the tile's own `onRemove`. Mining restriction now also matches 1.12.2: a pickaxe (any tier, wooden+) is required to yield drops, matching the 1.12.2 `Material.IRON` behaviour. Added both blocks to the `minecraft:mineable/pickaxe` tag and set `.requiresCorrectToolForDrops()` on their block properties. Added game tests `buildcraftunofficial:builder_drops_contents_and_self` and `buildcraftunofficial:architect_drops_contents_and_self` that simulate a survival-mode player with a wooden pickaxe running the full `playerWillDestroy → removeBlock → playerDestroy` flow and assert every expected item entity lands in the world.
- Restored Builder block functionality: the block now loads blueprints/templates from its snapshot slot, extracts matching items from its 27-slot resource inventory, pulls fluids from its 4 tanks (8 buckets each, via `FluidStacksResourceHandler`), and places blocks in-world using the already-ported `SnapshotBuilder` / `BlueprintBuilder` / `TemplateBuilder` pipeline. Owner `GameProfile` is now stored on place so `TemplateAPI`'s fake-player path works. The GUI gained the snapshot input slot, 27 resource slots, 24 read-only display slots (synced from the server over a container message so players see which blocks are still needed), 4 fluid tank widgets with tooltips, and left-to-break / left-to-place progress readouts. Also fixed the "blueprint door always open" visual defect by porting the 1.12.2 `SNAPSHOT_TYPE` blockstate property plus the three door submodels (`slot_empty` / `slot_blueprint` / `slot_template`) and composing them into the block's multipart blockstate.
- Architect Table: scan progress bar in the GUI now actually fills as blocks are captured (previously sampled an empty region of the texture atlas). Blocks currently being scanned are also briefly highlighted in-world with a translucent green "digitizing" cube that fades over ~2.5s, porting the 1.12.2 scan-feedback effect. Positions stream server→client in batches keyed to the architect's chunk, so the effect only transmits to nearby players.
- Architect Table GUI now shows a live 3D preview of the current scan-area contents before a blueprint is built. The preview refreshes while the GUI is open so block edits inside the marker volume surface within ~2 seconds; once a real blueprint exists in the output slot, that takes over as before.
- Blueprint tooltip preview now renders in a dedicated 100×100 panel below the item's main tooltip (matching the 1.12.2 separate-panel behavior), replacing the inline tooltip-image slot that left dead space alongside wider tooltip text. The panel uses the vanilla tooltip background so it picks up resource-pack theming; the 3D preview and rotation are unchanged.
- Blueprint/Template tooltip lines below the item name (blueprint name, hash, date, owner UUID) now render in gray, restoring the 1.12.2 look; modern Minecraft stopped auto-graying custom tooltip lines, which made them read too loud against the item title.
- Blueprint preview rendering now evaluates diffuse lighting in model space (per-frame UBO update compensating for the yaw/pitch transform), eliminating the brightness pulse on side faces as the preview rotated. Top faces render at full brightness, sides at ~60%, bottom at ~20% — close to the 1.12.2 look, with no orientation-dependent flicker.
- Blueprint preview now renders fluid cells (water, lava, oil, fuel, etc.) instead of silently skipping them. Source blocks draw as full textured cubes; flowing cells draw at their actual flow height, so captured cascades and pour streams read correctly in the preview the way they did in 1.12.2. Same-type fluid neighbours get shared-face culling so pool interiors show only the outer shell (no translucent-blend artifacts on water). Water uses the entity-translucent sheet; every other fluid uses entity-cutout so tinted BC fluids don't inherit water's semi-transparency.
- Fix: Architect Table now persists the contents of its input and output slots across chunk unloads / world saves. In 1.12.2 these were auto-persistent `ItemHandlerSimple` fields; the 26.1 port replaced them with raw `ItemStack` fields and forgot to add save/load hooks, so any clean blueprint left in the input or any used blueprint left in the output silently vanished on exit. The slot contents now round-trip via the tile's NBT like every other inventory.
- Fix: Completed the legacy `textures/blocks/` (plural, 1.12.2 convention) to `textures/block/` (singular, modern Minecraft) migration. Deleted 129 duplicate textures, moved 98 orphaned-legacy textures to the modern path, retargeted the `buildcraftunofficial:atlases/blocks.json` source to scan `block/` (removing the redundant `block/plugs` entry), and rewrote all remaining `buildcraftunofficial:blocks/` references across model/blockstate JSONs and Java (including `EnumEngineType` and two disabled renderers) to `buildcraftunofficial:block/`. Resolves missing-texture rendering for sprites such as the Architect Table's digitizing cube (`scan.png`) that the modern model references could not see.
- Fix: Fixed a NullPointerException crash when placing a blueprint in the architect table with no owner (e.g., in creative with no explicit owner set). The snapshot header serialization now uses a zero UUID as a fallback.
- Fix: Addressed an issue causing the BuildCraft Filler to incorrectly render the flying block animation when unpowered. This was caused by the client-side battery artificially gaining massive amounts of power during unpowered network syncs due to task cancellation logic repeatedly refunding extrapolated block tasks to the client's local read-only battery mirror.
- Fix: Filler flying block animation no longer loops when power is cut; blocks freeze in place matching 1.12.2 behavior. Server task data is now merged with max(client, server) power to prevent backward animation resets while maintaining smooth client-side extrapolation between 5-tick server syncs.
- Updated NeoForge to 26.1.2.12-beta and JEI to 29.5.0.26 for Minecraft 26.1.2.
- Fixed missing GUI textures for MJ Dynamo and FE Engine.
- Added tooltips for Redstone, Stirling, Combustion, and FE engines, as well as MJ Dynamo.
- Restored missing engine and dynamo block item translation strings by migrating to a centralized NeoForge ItemTooltipEvent registry.
- Added usage tooltips to the Land Mark and Path Mark blocks.
- Ported tooltips for the Filler, Builder, Architect Table, and Electronic Library from 1.12.2.
- Added tooltip to unpainted Paintbrushes to clarify they can strip paint from pipes.
- Added "Not usable in survival" tooltip to the Replacer block.
- Added usage tooltips to the Quarry and Mining Well blocks.
- Fixed an issue where the Filtered Buffer would not allow transport pipes to connect.
- Fixed a regression in the BuildCraft Filler where the robot and lasers would fail to render when unpowered or idle.
- Fixed a critical progression wipe in the BuildCraft Filler that caused it to forget state upon world reload or chunk unloads.
- Fixed a crash on world load caused by saving uninitialized Filler target layouts.
- Restored persistence of active Filler patterns and parameters, resolving a block entity network desync on world reload.
- Disabled directional shadowing on Marker and robot lasers to restore full emissive visibility even in the dark.
- Fixed an issue where dragging a pattern into the Filler GUI would visually revert back to None upon reopening the interface.
- Resolved a 'Network Protocol Error' crash in the internal server loop when attempting to override an active filler configuration with an invalid PlaceTask queue.
- Restored the classic 1.12.2 block-throwing placement animation for the BuildCraft Filler using a SubmitCustomGeometryEvent integration, bypassing 1.21.11 ItemRender constraints, and disabled the robot visualization during filling tasks for legacy parity. Fixes also included mitigating animation stutter by implementing client-side target power extrapolation.
Fix: Restored GUI slot background textures for the Filtered Buffer
Fix: Added gui.json atlas to correctly stitch slot sprite backgrounds
Fix: Corrected GUI atlas JSON path to merge with the vanilla minecraft namespace instead of creating a custom atlas
Fix: Corrected GUI icon placement and restored translucency/handling for the NOTHING filter icon in the Filtered Buffer
Fix: Restored Filtered Buffer ghost item rendering and dynamic ghost slot assignments
Fix: Re-calibrated opacity levels for Filtered Buffer ghost items and ?? icon to match legacy 30% presence thresholds
Fix: Restored Help and Ownership letgers to the Filtered Buffer GUI layout
Fix: Propagated BlockFilteredBuffer placement events down to TileFilteredBuffer, resolving Unknown ownership displays on freshly placed Filtered Buffers
Fix: Resolved generic return type conflicts in TileBC_Neptune network sync implementation to restore client-side owner mapping
Fix: Dynamically drew the block title onto the Filtered Buffer GUI, applying standard ARGB hex formatting to solve transparent text rendering errors
Enhancement: Fleshed out Filtered Buffer's ledger Help integrations with dummy bounding box hover tags and custom en_us localized guidance
Fix: Restored Shift-Click functionality to the Filtered Buffer by dropping broken placeholder implementations and permitting the generalized container logic to inherently read from the ItemHandlerFiltered ruleset
Fix: Fixed gates and wire objects failing to drop when their parent pipe is broken in survival mode
Enhancement: Implemented fully functional Help ledgers for the MJ Dynamo and FE Engine GUI
Fix: Enforced max 1 gear per upgrade slot on MJ Dynamo and FE Engine (4 slots = 4 gear max)
Test: Added EnergyConverterTester gametest suite for upgrade slot filtering and upgrade effectiveness on both machines
Fix: MJ Dynamo power ledger now displays RF output and RF stored instead of MJ, matching 1.12.2 behavior and showing the actual benefit of gear upgrades
Enhancement: Battery help ledgers on MJ Dynamo and FE Engine now dynamically display the current conversion rate based on installed gears, matching 1.12.2
Fix: Fixed a GUI bug where shift-clicking a stack into a max-1 slot bypassed the stack limits, and shift-clicking into an occupied slot permanently deleted the items
Fix: Fixed quarry rig collision phasing glitch caused by vanilla position-sync resetting the custom AABB to default 1x1 dimensions every few seconds
Fix: Fixed oil well generation for modern world depth (minY=-64): sphere cavity no longer clips into the bedrock gradient, oil tube now actually generates between the cavity and bedrock (was broken by negative length), and the tube no longer replaces bedrock blocks with oil which could expose the void
Fix: Fixed pump oil spring detection searching at Y=0 instead of the actual bedrock layer (minY=-64)
Fixed an issue where Markers would not drop themselves as items when broken or washed away by fluids. 
Enhancement: Restored full Filler block functionality: volume box detection on placement, TemplateBuilder integration for automated block placement/breaking, persistent 27-slot resource inventory, and owner tracking. The Filler GUI now opens when placed adjacent to volume markers.
Feature: Completely rebuilt the Filler GUI from the deprecated 1.12.2 JSON framework to the modern GuiBC8 system, restoring Pattern Statement slots, parameter fields, toggles, and the custom progress ledger readout
Fix: Resolved a compile error in TileFiller where generic types for the client rendering task lists conflicted with the TemplateBuilder's inner classes
Fix: Reordered the patterns in the Filler GUI to perfectly match the 1.12.2 layout
Fix: Restored the missing Ownership ledger to the Filler GUI and repaired the clipped vertical spacing/overlapping within the Filler's Progress ledger
Fix: Restored visual rendering of Gate-controlled machine mode icons (Loop/Off) in the Filler GUI
Fix: Fixed an issue where the Filler's Invert and Excavate GUI buttons required a world reload to recalculate building tasks by explicitly forcing immediate statement evaluation and syncing.
Fix: Fixed an issue where filling a stalled Filler's inventory would not cause it to immediately resume. Inventory interactions now correctly clear the builder's required resources cache and unset the completion flag.
Fix: Fixed a visual bug where the Filler's internal inventory count would flicker and ghost items would appear in the GUI. The root cause was the client-side network sync (every 5 ticks) calling cancel() which refunded reserved items back into the visible inventory, and then the Container sync immediately correcting them. Fixed by guarding the item refund to server-only, skipping redundant buildingInfo rebuilds on the client, and stripping inventory data from the block entity update packet.
- Fix: Restored legacy 1.12.2 block breaking particles and sound effects for the Filler (and other Builder logic) by migrating from world.removeBlock back to world.destroyBlock.
- Fix: Fixed a bug where players could not top up existing item stacks in the Filler's resource inventory via the GUI. The root cause was SlotBase.getMaxStackSize(ItemStack) returning remaining insertion capacity instead of total slot capacity, causing vanilla's container logic to reject items when the slot was partially filled. This affected all BuildCraft containers using SlotBase.
- Test: Added bulk insertion gameTests to verify 4 gears can be distributed across FE Engine and MJ Dynamo upgrade slots automatically.
- Fix: Enforced maximum stack size of 1 on the FE Engine and MJ Dynamo GUI upgrade slots, preventing mouse dropping or shift-clicking an entire stack of gears into a single slot.
- Fix: Fixed a bug where the Filler would never stop operating after completing its task (e.g. clearing a volume), continuously destroying any blocks placed within the area. The missing isFinished() guard from a 1.12.2 TODO has been restored, so the Filler now correctly halts when mode is ON and the pattern is complete, while still looping when mode is LOOP.
- Fix: Fixed a bug where the Filler would falsely report itself as finished (is_finished = true) before it had actually scanned and processed the entire volume. The SnapshotBuilder only checks 10 blocks per tick, but the isDone logic was ignoring unchecked (UNKNOWN) blocks and in-progress tasks, causing it to prematurely declare completion after just a handful of blocks were processed.
- Fix: Fixed a legacy 1.12.2 bug where Pipe Gate actions that set the Filler's pattern (e.g., "Filler Action: Box") would lock the GUI visually, but failed to actually update the Filler's physical logic or prevent the player from forcefully overwriting the task in the interface. The GUI now strictly prevents interacting with the Pattern Statement slot while locked, the Server actively rejects forced manipulation packets, and the Builder correctly initiates the gate-assigned task upon receiving it.
- Fix: Fixed the Gate connector button (the vertical link between logic slots on Iron AND and higher gates) being a no-op. AbstractContainerScreen.mouseClicked() was swallowing the click before the connector hit-test could run. Reordered mouse handling to check connector buttons first and added optimistic client-side state update for instant visual feedback. Also fixed initial connection state sync when the Gate GUI is opened.

- Replaced deprecated AbstractContainerScreen GUI getter methods with their modern getters to fix compilation removal warnings.
- Enhancement: Ported the contextual Help Ledger text for the Filler block (Excavate, Invert, Mode, Locked) from 1.12.2.
- Fix: Restored the 1.12.2 Filler inventory filter that only accepts block-type items. Previously, any item (swords, sticks, ingots, etc.) could be placed into the Filler's resource slots, causing it to attempt building with unbuildable items. The filter now checks for BlockItem instances, matching the original ItemBlocks.getList() behavior.
- Test: Added FillerInventoryTester game test to verify both block items are accepted and non-block items are rejected by the Filler's inventory.
- Fix: Fixed a missing rendering implementation for the Architect Table where the "blue caution lasers" indicating the selected building volume bounds were not rendering on the client when placed next to volume markers. State synchronization and block entity lifecycle registration have been implemented, bringing back the volume visualizations.
