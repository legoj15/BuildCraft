- The Zone Planner's map zooms out to classic 1.7.10's survey range again: keep scrolling and the window spans about 1,700 blocks across (8 blocks per step), where the 8.0.x rewrite it was revived from stopped at roughly a tenth of that. Zoomed out that far the map samples the terrain on a coarse grid (one merged column per 2/4/8 blocks) so the view stays smooth instead of emitting a sub-pixel quad per block, the cursor keeps tracking the terrain, and terrain shows wherever the client has the chunks loaded. Zooming out also steps further per scroll notch than zooming in, so the survey view is a few quick flicks away.
- The Zone Planner's help ledger now actually documents the screen: the map window, the 16-slot paintbrush rack, the zone-input row and the zone-output column each highlight and explain themselves (with a short hover tooltip apiece) — what the brush rack is for, how copying a zone from a Map Location into the planner works, and how writing a stored zone back out onto one works. Previously only the map window was described.

- The "BuildCraft Robots" tab lists its robots and boards in the exact order classic 1.7.10 BuildCraft showed (lumberjack and planter leading, bomber last) instead of a shuffled-looking order. Classic BuildCraft's order was itself a loading accident — it registered its boards by cost tier but displayed them in whatever sequence the JVM's hashmap landed on — so the port now pins that observed order deliberately, and future boards get an explicit slot in it rather than a random one.
- Programmed robots and redstone boards label themselves like 1.7.10 again: the board's name line in the tooltip is bold, and the names drop the "Board" word ("Lumberjack", "Tank", "Leaf Cutter" — not "Lumberjack Board"). The descriptions underneath are unchanged.
- Fixed the Requester item rendering as the missing-texture checkerboard in inventories and the creative tab on Minecraft 1.21.10 and newer — it shipped without the client item definition those versions require, so its block model never loaded there.
- The Builder robot is back, rebuilt for the modern snapshot system: a robot with a Builder Board finds the nearest Builder machine whose blueprint is still missing materials, fetches exactly what it needs from a supply station, flies over and stocks the machine's resource grid — the robotic supply line for the Builder that classic BuildCraft's construction markers used to be. It searches up to three 64-block radii (the classic range), works inside a work zone if you set one, and sleeps when no Builder needs anything. It's the most expensive board in the game at 512,000 RF (51,200 MJ) on the classic yellow chip — the fifth tier colour, back with this board.
- Four more classic robot programs join it: the Leaf Cutter (shears leaves off trees, blue chip), the Shovelman (shovels dirt, sand, gravel, clay, farmland, grass and snow, blue chip), the Bomber (loads TNT, flies 20 blocks over a random spot of ground and drops primed TNT, red chip) and the Stripes board (a mobile stripes pipe — it grabs any item, flies to an empty cell and "uses" the item there through the stripes handlers: plant a seed, till with a hoe, shear a sheep, whatever the item does on a stripes pipe; yellow chip). Each has its classic robot skin and board chip, on every Minecraft version.
- Robots can wear things again: right-click a robot holding a helmet or any mob head and it puts it on (up to eight pieces), exactly like classic BuildCraft — worn armour soaks robot damage, the pieces drop when the robot is destroyed, and a sneak-wrench peels them off one at a time before dismantling the robot. Worn items also count for gate filters again (Forbid/Force Robot matching a robot by what it wears). Worn pieces render as a small item column above the robot.
- Fixed the Pump robot being broken on Minecraft 1.21.10 and newer: it could never move a single drop of fluid (an internal error every cycle). Also fixed on those versions: every robot mined at less than a fifth of its proper speed — the tool's dig speed was silently dropped out of the block-breaking math, so a diamond-pickaxe robot broke stone 25 times slower than it should (the same bug also means tools now count for harvest checks exactly as they should).
- Tool-using robots (Miner, Lumberjack, Leaf Cutter, Shovelman, and anyone else breaking blocks) now mine at true tool speed on every Minecraft version: a diamond pickaxe gets through stone in six swings, not 151.
- The classic "leaves" and "shoveled" block categories are back for the new boards: leaves covers every tree's foliage, shoveled covers the shovel-diggable ground (dirt family, grass, sand, red sand, gravel, clay, farmland and the snow layer).
- The request network is back: place a Requester beside a pipe with a robot station, set what you want and how much of it (click a ghost slot holding the number of items you want to order), and a Delivery robot does the rest — it claims the order, fetches the goods from wherever a supply station offers them, flies back and hands them over. The Requester keeps each order in its own slot and only ever accepts the item (and amount) that slot asked for, so pipes and hoppers can top a partly-filled order up but can never dump the wrong thing into it. The classic recipe is back too (iron, a piston, an iron gear, a chest and redstone), and the Request Needed Items gate action works again — a gate holding it with an item parameter makes its station request that stack with no Requester block at all, exactly like classic BuildCraft.
- The Programming Table is back: boards are survival-craftable at last. Insert any redstone board, click a program in the grid (every registered board, cheapest first — addon boards included), and feed it laser power: green-tier boards cost 800 MJ, the blue work boards 3200 MJ, the Knight 12,800 MJ, exactly like classic BuildCraft. New crafting recipes go with it: the table itself (obsidian, an emerald, a redstone chipset and a diamond gear), a blank board (8 paper around a redstone dust) and the robot chassis (5 iron, redstone, 2 diamond chipsets) — all classic recipes. The table and the Integration Table appear on the BuildCraft Robots tab.
- The Integration Table ships for real, no longer a dev-only shell: put a robot in the centre, a redstone board beside it, and 5000 MJ of laser power — out comes the programmed robot, carrying whatever charge it had (a fully drained one gets topped up just enough to fly home and reach a dock). Landing its first recipe fixed two bugs it had been hiding as dev-only content: items carrying data (like robots) could never craft at all, and a failed ingredient check could hand you the output while leaving every input untouched.
- Robots can be named with name tags again (7.2.x parity) — a named robot shows its name overhead on every Minecraft version line.
- Blank robots and blank redstone boards stack to 16 again while programmed ones stay at 1, like classic BuildCraft.
- Robots wear their skins in the classic orientation again. The in-world robot's four side faces were rendering upside down compared to a 7.1.x build — each side's transparent band hugged the cube's bottom edge instead of the top, the little red charge lights sat visibly too high, and per-kind side markings could show mirrored or on the wrong flank. The cause: 8.0.x's model re-cut baked the modern model-JSON face conventions in, while 7.1.x drew the robot with the old model-box mapping. Both the in-world renderer and the item model now reproduce the 7.1.x look exactly (eyes-on-the-underside and all — that really is where classic BuildCraft drew the eye decal).
- Falling into crude oil now breaks your fall on Minecraft 26.1.2 too, as on 26.2 and like falling into water — the NeoForge update there fixed modded, non-water-like fluids not cushioning landings at all. The 1.21.x versions still land hard in oil; their NeoForge lines haven't picked the fix up.
- A robot's icon now shows its charge at a glance — no more mousing over to tell a full robot from an empty one. The eyes use 7.1.x's own overlay art and behaviour, which the inventory icon never had (upstream hardcoded the icon to look fully charged): they now fade from dark at 0% to bright red at full, exactly like a robot's eyes in the world always have. The classic pale-cyan charging port on the robot's underside is drawn too, always on, as upstream drew it. Both show in inventories, hands, item frames, and on dropped stacks. Robots also show the standard durability-style charge bar under their icon (empty shows the bare trough, full draws all 13 px), in the classic readout colours — dark red empty through green full — the same colours the charge tooltip text has always used. The empty-board template robot shows none of it — no charge tooltip, no bar, and dark eyes — because a blank chassis with no program is not a functional robot and holds nothing worth reading; classic BuildCraft hid the charge line on it too.
- Robots in your inventory, on the ground, and in item frames are drawn at their classic size again — they had been rendering far smaller than classic BuildCraft showed them. Robots held in hand and robots in the world are unchanged.
- Minecraft 26.2: BuildCraft now shows its square gear icon in the mods list (the revamped screen expects one and previously showed none), and its classic logo as the wide banner in the mod details. The new properties are declared alongside the old one, so older Minecraft versions are unchanged.
- Fixed the "BuildCraft Robots" tab's icon: it was a data-less board stack, which rendered as the wrong blank PCB (the 1.12.2-era art) instead of the classic Empty Board chip — the same reason a plain `/give`d board looked different from the creative-tab one. The tab now shows a proper Empty Board, and boards with no or unknown board data render as the Empty Board on every Minecraft version, exactly as classic BuildCraft's registry fallback did — so there is only one Empty Board look again.

- Every robot in the "BuildCraft Robots" tab wears its own colour again: each ready-made robot shows the per-kind skin it wears in the world (blue picker, red knight, and so on) instead of all stacks drawing as the same white chassis, and the empty board's clean chip is distinct again. On Minecraft 1.21.1 the redstone boards also show their tier colours (green/blue/red/clean) like they already do on the newer Minecraft versions.

- The Robot Station is called "Docking Station" again, matching classic BuildCraft. Only the name changed — the item, its recipe, and existing worlds are unaffected.
- Robots have their own creative tab again, like classic BuildCraft: "BuildCraft Robots" holds the zone planner, every ready-made robot, the robot station, and every redstone board — the robot station no longer hides in the Pluggables tab.
- Minecraft 1.21.1: fragile fluid shards and all BuildCraft buckets (oil and fuel variants) were drawn far too large whenever held, dropped, worn, or shown in an item frame — they now render at the standard item size like they do on the other Minecraft versions.
- A robot that loses its home station now shuts down and drops to the ground, the way it always did in classic BuildCraft — pick it back up with a sneak-click of a wrench. Previously it carried on working with no home at all, still reporting itself docked at a station that had been destroyed. Robots that never had a home station (spawned by command) still simply idle.
- A robot station no longer haunts the spot where its pipe used to be. If a pipe was removed by anything other than a player's hand — a command, a piston, an explosion, another mod's tool — the station stayed registered, so robots kept flying to a station floating in mid-air.
- Robots can unload fluid at an ordinary station again. A Pump or Tank robot could only empty its tank into a pipe that happened to have something connected to the face directly opposite the station, so at a normal dead-end dock the unload silently never happened.
- A "Go to Station" gate action with no destination set now sends the docked robot to the gate's own station, as it did in classic BuildCraft, instead of doing nothing at all.
- A programmed robot's tooltip now names its program and describes what it does, above the charge readout — a shelf of robots used to be indistinguishable. Blank robots show nothing at all — no program means no readout, as in classic BuildCraft.
- The "Filter" and "Filter Tool" gate actions now actually steer the work robots. A Filter on a robot's home station narrows what its Lumberjack, Miner or Harvester will go looking for and which seed its Planter will fetch; a Filter Tool says which tool any robot may pick up. A station with no filter set still lets its robots do anything, as before, and the gate can be re-parameterised while robots are working.
- Robots that fetch something to hold — a seed, a tool — now take a single item from the station instead of a whole stack. A Planter fed from a chest of seeds was picking up 64, planting one, and dropping the other 63 on the ground every trip.
- Knight robots now fight every hostile mob, not just the ones that happen to walk. Slimes, magma cubes, ghasts, phantoms, shulkers and the ender dragon were all invisible to a Knight before, so a Knight posted in a slime farm or the Nether simply stood there.
- Gate parameter slots that hold an item can be filled in again: clicking one of the plain item slots on a gate action (the robot Filter and Filter Tool actions, Provide/Accept Items, Provide/Accept Fluids, Go to Station) with an item on the cursor now sets that slot to the item you are holding, and clicking with an empty hand clears it. Previously the slot ignored what you were holding entirely, so those filters could never be set or cleared.
- A robot sent to claim a new home station now lines up two blocks out before slotting in, as it used to, instead of stopping right against the station.
- The Harvester robot finds and cuts cactus and sugar cane again (it takes the upper segments and leaves the base rooted), and it no longer mows down flowers — a lone poppy or dandelion was never a crop in classic BuildCraft, and planted flower beds are now safe from it.
- A sleeping robot now obeys the Wake Up gate action on its own home station, wherever it happens to be parked. It previously only listened to the station it was sitting at, so a Wake Up gate at home did nothing once the robot had wandered off.
- The Pump robot now respects the work filter on its home station: put a lava bucket in a Filter gate action and the robot only pumps lava instead of grabbing the nearest water pool. With no filter set it still pumps any fluid source it finds.
- Robot stations with more than one Provide Items gate action work again: a station offering, say, dirt from one action and stone from another handed out nothing at all, because every filtered action had to agree. Now the station supplies whatever any of its filtered Provide Items actions offers.
- Fixed a server crash: a Provide Fluids (or Accept Fluids) gate on a robot station that had no wooden fluid pipe feeding it would crash the server as soon as any fluid-carrying robot looked for somewhere to load. Such a station is now simply skipped.
- The Tank robot program is in: a Tank Board robot is the fluid carrier — it loads fluid at a supply station (a robot station on a wooden fluid pipe pointed at a tank), hauls up to four buckets in its internal tank, and unloads into the pipe network at another station. Like the item Carrier it needs a Provide Fluids gate on the supply station (and an Accept Fluids gate to unload); a "Filter"-style work filter holding a filled bucket narrows what it will load. It joins the other boards in the creative tab with its green chip and its own robot skin. It also uncovered a real robot bug: a robot's tank could gain (or lose) fluid during a station search's dry run instead of only when actually loading or unloading — searches are now purely observational.
- New config option `disableFacadeGeneration` (Common → general): turns BuildCraft's automatic facade generation completely off — no startup block scan, no generated assembly recipes, no facade variants in the creative tab, no JEI entries, and no client-side texture-dedup scan. Meant for players using another universal-facades mod. Facades already placed in a world keep their looks, and the (now empty) BuildCraft Facades tab stays registered; takes effect after a restart. (#29)
- Robot station gates are functional: all 22 robot/station triggers and actions from classic BuildCraft work again — the sleep trigger and wake-up action, the in-station and linked triggers, go-to-station and work-in-area actions with map-location zones, and the item/fluid filters that decide what a robot may take from or leave at a station. Gates now also govern robot behaviour the way they always should have: a station with no gate refuses robot item and fluid transfers instead of letting any robot use it freely, so a station you want a robot to use needs an Accept/Provide Items (or fluids) gate.
- Minecraft 1.21.10/1.21.11: the Planter robot no longer recognises seeds there (a version-cliff bug since the boards shipped) — it plants crops on those versions like it does on 26.x.
- Minecraft 1.21.1: heat-exchanger output slots cap at one bucket again — a hardcoded slot limit of 64 let a click or shift-click stack a whole bucket pile into a single-bucket slot.
- Eight more robot programs: Lumberjack (breaks logs), Miner (mines ores, and only what its tool can actually mine), Harvester (harvests mature crops), Planter (plants seeds), Farmer (hoes dirt for crops), Pump (moves fluids between the robot's tank and its station), Knight (fights mobs), and Butcher (hunts animals). They join Picker and Carrier in the creative tab — each as its own board with a tier-colored chip (green Picker/Carrier, blue work boards, red Knight) and as ready-made robots.
- Fixed: animals no longer float up out of crude oil — they sink and stay put like they should, since oil is too thick to swim in. The fix now reaches Minecraft 26.1.2 too, where it hadn't before. (On 26.1.2 the same change also means oil pools no longer cushion falls; oil isn't water.)
- Falling into crude oil now breaks your fall on Minecraft 26.2 — drop into an oil pool from any height and you land unharmed, like into water. Oil currents also push you along in shallow oil now, not only when you're fully submerged.
- Robots finally have work: the first two robot programs (Picker and Carrier boards) are in. A Picker robot flies around picking up dropped items within 250 blocks and unloads them at a station; a Carrier robot loads cargo from a supply station (a robot station on a wooden pipe) and unloads it into the pipe network there — carrying it to a *different* station arrives with the gate actions in a later phase. An un-programmed (empty) robot now flies home to its station and parks instead of idling in place. Robots also recharge themselves now: a robot that runs low flies to any power-providing station on its own and tops itself up. (Boards are creative-tab only for now — the programming table that makes them in survival arrives later.)
- Robots are back — the first working piece of them. A Robot itself now exists: place the (creative-only, for now) Robot item on a free Robot Station and it docks there, charges from the pipe network, and shows its charge as a glowing overlay. Robots have no work programs yet — those come next — so a placed robot idles at its station. Damage drains a robot's charge instead of health; a robot that runs out is destroyed and drops itself (keeping its remaining charge) along with anything it was carrying. Sneak-click with a wrench to peel a robot off its station and back into your inventory.
- The Robot Station can now be crafted (3 iron ingots + 1 Gold Chipset) — it was accidentally creative-only when it shipped.
- Robot Stations look like they used to again: the classic two-part pedestal — a plate against the pipe with a short post standing out from it — instead of the plain stub they had been. Their status colour now reads from any angle, not just head-on: green for a free station, yellow once a robot has claimed one, red for a robot's home station. The station's outline also grew to match, so it's easier to click.
- For addon authors: the robot API changed shape while it was still unused — robots are plain entities rather than living ones, AIs program against the new `IRobotAccess` interface, a robot's maximum charge doubled to match the classic 1 MJ = 10 RF conversion, and 7.1.x's backwards `isActive()` (which returned true for a *sleeping* robot) is now honestly named `isSleeping()`.
- The mod jar now includes its license texts (`META-INF/LICENSE` and `META-INF/LICENSE.API`). The jar previously shipped with none, even though parts of the BuildCraft API it contains are distributed under the MIT license, which requires the notice to travel with the code.
- Added the Robot Station pluggable: place it on a pipe to create a docking station (`buildcraftunofficial:robot_station`). A docked robot charges directly from the pipe network; the station's indicator shows available/reserved/linked. (First piece of the in-progress robotics restoration — no robots to dock yet.)
- Spectators can now view Assembly Table, Advanced Crafting Table, and Integration Table interfaces
- Added a complete Simplified Chinese (`zh_cn`) translation (AI/machine assisted), thank you [beizhou1](https://github.com/beizhou1)!  
  - 如发现翻译不准确，请提交拉取请求或反馈问题
- A large amount of text that was hardcoded in English now goes through the language files
- The Filler and Filler Planner now explain their pattern slot and pattern parameters in the help ledger — previously the two most important controls on those screens were the only ones with no help. The Filler Planner also regained its help ledger, which had been switched off by mistake.
- With RF unit names enabled, the FE Engine's upgrade help, the FE limiter pipe's gate actions, and the guide book's pipe chapter now say "RF" like the rest of the interface instead of remaining "FE".
- The Replacer's help now states that the single-block schematics are not consumed, which differs from older BuildCraft versions.
- The help ledger shows a warning icon when a screen has no help available, instead of opening an empty panel that looked identical to a working one.
- The Fragile Fluid Container's contents now follow your unit display settings rather than always showing raw millibuckets.
- Removed the non-functional Guide Note item (a creative-only stub that never opened anything, in this port or 1.12.2).