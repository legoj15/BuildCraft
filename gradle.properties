# Properties from NeoForge 26.1 MDK
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Minecraft and NeoForge versions
minecraft_version=26.1.2
neo_version=26.1.2.70-beta

# Dependency version ranges injected into neoforge.mods.toml. These are the
# 26.1.2-node defaults; each Stonecutter node overrides them in versions/<node>/gradle.properties.
neo_dep_range=[26.1.2.21-beta,)
mc_dep_range=[26.1.2,26.2)

#Intentionally different and unjoined from the current minecraft version
mod_version=2026.1.0-rc2

# Git branch hosting the published BuildCraftVersions.json (the in-game update
# check reads it). All MC nodes now share one branch: main.
update_branch=main

# Dependencies
jei_version=29.5.0.26
